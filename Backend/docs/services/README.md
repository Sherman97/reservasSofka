# Documentacion por Servicio

Esta carpeta centraliza la documentacion funcional y tecnica extraida desde `Backend/services`.

Estructura:

- `api-gateway/`
- `auth-service/`
- `bookings-service/`
- `database/`
- `inventory-service/`
- `locations-service/`
- `notifications-service/`

Nota:
- Se excluyeron archivos temporales o de salida de build para conservar solo documentacion importante.
